#include<iostream>
//#include<string>
#include<iomanip>// header or library for manipulators
using namespace std;

int main() {
    // manipulators in c++
    // int a=10, b=25, c=1345;
    // cout<<"The value of a is: "<<setw(10)<<a<<endl;
    // cout<<"The value of b is: "<<setw(4)<<b<<endl;
    // cout<<"The Value of c is: "<<setw(15)<<c<<endl;
   // like setw,setprecision, setfill

   // operator precidence
   //In C++, operator precedence determines the order in which operations are performed in an expression. Operators with higher precedence are evaluated before operators with lower precedence.

// Here is a list of the operator precedence in C++, from highest to lowest:

// Scope resolution (::)
// Member selection (.*, ->*)
// Postfix increment and decrement (++, --)
// Prefix increment and decrement (++, --)
// Unary plus and minus (+, -)
// Logical negation (!)
// Address-of (&)
// Dereference (*)
// Type cast ((type))
// Multiplication, division, and modulus (*, /, %)
// Addition and subtraction (+, -)
// Shift operators (<<, >>)
// Relational operators (<, <=, >, >=)
// Equality operators (==, !=)
// Bitwise AND (&)
// Bitwise XOR (^)
// Bitwise OR (|)
// Logical AND (&&)
// Logical OR (||)
// Conditional (?:)
// Assignment (=, +=, -=, *=, /=, %=, &=, |=, ^=, <<=, >>=)
// Comma (,)
//For example, in the expression a + b * c, the multiplication operator (*) has higher precedence than the addition operator (+), so b * c is evaluated first.


    return 0;


}